# Local-only data and recovery snapshots must never be uploaded with a deploy.
backups/

# Vercel installs dependencies and builds these artifacts itself.
node_modules/
dist/
coverage/

# Local configuration and tooling state.
.git/
.vercel/
.env*
*.log
